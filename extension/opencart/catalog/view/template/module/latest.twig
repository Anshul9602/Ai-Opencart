<section class="ai-section">
  <div class="container">
    <div class="ai-section-header ai-animate">
      <span class="ai-label">Just Arrived</span>
      <h2>{{ heading_title }}</h2>
      <p>Fresh additions to our collection</p>
    </div>
    <div class="row{% if axis == 'horizontal' %} row-cols-1 row-cols-sm-2 row-cols-md-3 row-cols-xl-4{% endif %} g-4">
      {% for product in products %}
        <div class="col">{{ product }}</div>
      {% endfor %}
    </div>
  </div>
</section>
